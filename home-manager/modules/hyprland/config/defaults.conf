# ┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
# ┃                     Defaults Configuration                  ┃
# ┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

$filemanager = dolphin
# $applauncher = rofi -show combi -modi window,run,combi -combi-modi window,run
$applauncher = rofi -show run -modi window,run,combi -combi-modi window,run
# $background_img = $HOME/Pictures/wallpaper/background/dark_tree.png
$background_img = $HOME/Pictures/wallpaper/lock_screen/crossing_in_japan.jpg
$terminal = ghostty
$idlehandler = swayidle -w timeout 300 'swaylock -f -c 000000' before-sleep 'swaylock -f -c 000000'
$capturing = grim -g "$(slurp)" - | swappy -f -
